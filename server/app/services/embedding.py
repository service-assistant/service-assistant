# TODO: fill these function and use correct types (not int and str)


def embed_question(question: str) -> list[int]:
    return [1, 2, 3]


def get_close_chunks(embedded_vector: list[int]) -> list[str]:
    return ["a", "b", "c"]
