name: Main workflow

on:
  push:
    branches:
      - "staging"
  pull_request:
    branches:
      - "**"

jobs:
  app-lint:
    name: Lint (app)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node
      - name: Run lint check
        run: npm --prefix ./app run lint

  app-format-check:
    name: Check formatting (app)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node
      - name: Run format check
        run: cd app && npm exec prettier -- --check .

  app-test:
    name: Run tests (app)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node
      - name: Run tests
        run: npm --prefix ./app test

  app-typecheck:
    name: Type check (app)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node
      - name: Run type check
        run: npm --prefix ./app run typecheck

  admin-lint:
    name: Lint (admin)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node
        with:
          working-directory: ./admin
      - name: Run lint check
        run: npm --prefix ./admin run lint

  admin-format-check:
    name: Check formatting (admin)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node
        with:
          working-directory: ./admin
      - name: Run format check
        run: npm --prefix ./admin run format-check

  admin-typecheck:
    name: Type check (admin)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node
        with:
          working-directory: ./admin
      - name: Run type check
        run: npm --prefix ./admin run typecheck

  admin-test:
    name: Run tests (admin)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node
        with:
          working-directory: ./admin
      - name: Run tests
        run: npm --prefix ./admin test

  debug-lint:
    name: Lint (debug)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node
        with:
          working-directory: ./debug
      - name: Run lint check
        run: npm --prefix ./debug run lint

  debug-format-check:
    name: Check formatting (debug)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node
        with:
          working-directory: ./debug
      - name: Run format check
        run: npm --prefix ./debug run format-check

  debug-typecheck:
    name: Type check (debug)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node
        with:
          working-directory: ./debug
      - name: Run type check
        run: npm --prefix ./debug run typecheck

  debug-test:
    name: Run tests (debug)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node
        with:
          working-directory: ./debug
      - name: Run tests
        run: npm --prefix ./debug test

  api-lint:
    name: Lint (api)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-python-poetry
      - name: Install dependencies
        run: poetry --directory ./api install --all-groups
      - name: Run lint check
        run: poetry --directory ./api run ruff check app tests

  api-format-check:
    name: Check formatting (api)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-python-poetry
      - name: Install dependencies
        run: poetry --directory ./api install --all-groups
      - name: Run format check
        run: poetry --directory ./api run ruff format --check app tests

  api-typecheck:
    name: Type check (api)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-python-poetry
      - name: Install dependencies
        run: poetry --directory ./api install --all-groups
      - name: Run type check
        run: poetry --directory ./api run pyright

  api-test:
    name: Run tests (api)
    runs-on: ubuntu-24.04
    services:
      postgres:
        image: pgvector/pgvector:pg18-trixie
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: service_assistant_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-python-poetry
      - name: Install dependencies
        run: poetry --directory ./api install --all-groups
      - name: Run tests
        run: poetry --directory ./api run pytest
        env:
          ENV: test
          POSTGRES_PASSWORD: postgres
