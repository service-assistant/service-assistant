services:
    app:
        build:
            context: .
            args:
                EXPO_PUBLIC_API_URL: ${EXPO_PUBLIC_API_URL}
        ports:
            - '127.0.0.1:${PORT}:80'
        restart: unless-stopped
